# Project Overview

Build a self-hosted, Google Photos-like service for a single shared library (me + wife).
Primary goals: fast browsing, easy album sharing, robust video playback, and simple ingest via watched folders.

Scope is intentionally smaller than Google Photos: no multi-tenant accounts, no AI/face recognition, no in-app edits.
